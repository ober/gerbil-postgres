(declare (block) (standard-bindings) (extended-bindings))
(begin
  (begin
    (load-module "std/db/dbi")
    (load-module "std/error")
    (load-module "std/sugar"))
  (load-module "jafourni/gerbil-postgres/errors~0"))
